// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"

	"git.las.iastate.edu/SeniorDesignComS/2023spr/online-certificate-repo/backend/pkg/database/ent/attributetypehistory"
	"git.las.iastate.edu/SeniorDesignComS/2023spr/online-certificate-repo/backend/pkg/database/ent/predicate"
)

// AttributeTypeHistoryQuery is the builder for querying AttributeTypeHistory entities.
type AttributeTypeHistoryQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.AttributeTypeHistory
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AttributeTypeHistoryQuery builder.
func (athq *AttributeTypeHistoryQuery) Where(ps ...predicate.AttributeTypeHistory) *AttributeTypeHistoryQuery {
	athq.predicates = append(athq.predicates, ps...)
	return athq
}

// Limit the number of records to be returned by this query.
func (athq *AttributeTypeHistoryQuery) Limit(limit int) *AttributeTypeHistoryQuery {
	athq.ctx.Limit = &limit
	return athq
}

// Offset to start from.
func (athq *AttributeTypeHistoryQuery) Offset(offset int) *AttributeTypeHistoryQuery {
	athq.ctx.Offset = &offset
	return athq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (athq *AttributeTypeHistoryQuery) Unique(unique bool) *AttributeTypeHistoryQuery {
	athq.ctx.Unique = &unique
	return athq
}

// Order specifies how the records should be ordered.
func (athq *AttributeTypeHistoryQuery) Order(o ...OrderFunc) *AttributeTypeHistoryQuery {
	athq.order = append(athq.order, o...)
	return athq
}

// First returns the first AttributeTypeHistory entity from the query.
// Returns a *NotFoundError when no AttributeTypeHistory was found.
func (athq *AttributeTypeHistoryQuery) First(ctx context.Context) (*AttributeTypeHistory, error) {
	nodes, err := athq.Limit(1).All(setContextOp(ctx, athq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{attributetypehistory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (athq *AttributeTypeHistoryQuery) FirstX(ctx context.Context) *AttributeTypeHistory {
	node, err := athq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AttributeTypeHistory ID from the query.
// Returns a *NotFoundError when no AttributeTypeHistory ID was found.
func (athq *AttributeTypeHistoryQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = athq.Limit(1).IDs(setContextOp(ctx, athq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{attributetypehistory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (athq *AttributeTypeHistoryQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := athq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AttributeTypeHistory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AttributeTypeHistory entity is found.
// Returns a *NotFoundError when no AttributeTypeHistory entities are found.
func (athq *AttributeTypeHistoryQuery) Only(ctx context.Context) (*AttributeTypeHistory, error) {
	nodes, err := athq.Limit(2).All(setContextOp(ctx, athq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{attributetypehistory.Label}
	default:
		return nil, &NotSingularError{attributetypehistory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (athq *AttributeTypeHistoryQuery) OnlyX(ctx context.Context) *AttributeTypeHistory {
	node, err := athq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AttributeTypeHistory ID in the query.
// Returns a *NotSingularError when more than one AttributeTypeHistory ID is found.
// Returns a *NotFoundError when no entities are found.
func (athq *AttributeTypeHistoryQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = athq.Limit(2).IDs(setContextOp(ctx, athq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{attributetypehistory.Label}
	default:
		err = &NotSingularError{attributetypehistory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (athq *AttributeTypeHistoryQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := athq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AttributeTypeHistories.
func (athq *AttributeTypeHistoryQuery) All(ctx context.Context) ([]*AttributeTypeHistory, error) {
	ctx = setContextOp(ctx, athq.ctx, "All")
	if err := athq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AttributeTypeHistory, *AttributeTypeHistoryQuery]()
	return withInterceptors[[]*AttributeTypeHistory](ctx, athq, qr, athq.inters)
}

// AllX is like All, but panics if an error occurs.
func (athq *AttributeTypeHistoryQuery) AllX(ctx context.Context) []*AttributeTypeHistory {
	nodes, err := athq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AttributeTypeHistory IDs.
func (athq *AttributeTypeHistoryQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if athq.ctx.Unique == nil && athq.path != nil {
		athq.Unique(true)
	}
	ctx = setContextOp(ctx, athq.ctx, "IDs")
	if err = athq.Select(attributetypehistory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (athq *AttributeTypeHistoryQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := athq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (athq *AttributeTypeHistoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, athq.ctx, "Count")
	if err := athq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, athq, querierCount[*AttributeTypeHistoryQuery](), athq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (athq *AttributeTypeHistoryQuery) CountX(ctx context.Context) int {
	count, err := athq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (athq *AttributeTypeHistoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, athq.ctx, "Exist")
	switch _, err := athq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (athq *AttributeTypeHistoryQuery) ExistX(ctx context.Context) bool {
	exist, err := athq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AttributeTypeHistoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (athq *AttributeTypeHistoryQuery) Clone() *AttributeTypeHistoryQuery {
	if athq == nil {
		return nil
	}
	return &AttributeTypeHistoryQuery{
		config:     athq.config,
		ctx:        athq.ctx.Clone(),
		order:      append([]OrderFunc{}, athq.order...),
		inters:     append([]Interceptor{}, athq.inters...),
		predicates: append([]predicate.AttributeTypeHistory{}, athq.predicates...),
		// clone intermediate query.
		sql:  athq.sql.Clone(),
		path: athq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		HistoryTime time.Time `json:"history_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AttributeTypeHistory.Query().
//		GroupBy(attributetypehistory.FieldHistoryTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (athq *AttributeTypeHistoryQuery) GroupBy(field string, fields ...string) *AttributeTypeHistoryGroupBy {
	athq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AttributeTypeHistoryGroupBy{build: athq}
	grbuild.flds = &athq.ctx.Fields
	grbuild.label = attributetypehistory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		HistoryTime time.Time `json:"history_time,omitempty"`
//	}
//
//	client.AttributeTypeHistory.Query().
//		Select(attributetypehistory.FieldHistoryTime).
//		Scan(ctx, &v)
func (athq *AttributeTypeHistoryQuery) Select(fields ...string) *AttributeTypeHistorySelect {
	athq.ctx.Fields = append(athq.ctx.Fields, fields...)
	sbuild := &AttributeTypeHistorySelect{AttributeTypeHistoryQuery: athq}
	sbuild.label = attributetypehistory.Label
	sbuild.flds, sbuild.scan = &athq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AttributeTypeHistorySelect configured with the given aggregations.
func (athq *AttributeTypeHistoryQuery) Aggregate(fns ...AggregateFunc) *AttributeTypeHistorySelect {
	return athq.Select().Aggregate(fns...)
}

func (athq *AttributeTypeHistoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range athq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, athq); err != nil {
				return err
			}
		}
	}
	for _, f := range athq.ctx.Fields {
		if !attributetypehistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if athq.path != nil {
		prev, err := athq.path(ctx)
		if err != nil {
			return err
		}
		athq.sql = prev
	}
	return nil
}

func (athq *AttributeTypeHistoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AttributeTypeHistory, error) {
	var (
		nodes = []*AttributeTypeHistory{}
		_spec = athq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AttributeTypeHistory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AttributeTypeHistory{config: athq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(athq.modifiers) > 0 {
		_spec.Modifiers = athq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, athq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (athq *AttributeTypeHistoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := athq.querySpec()
	if len(athq.modifiers) > 0 {
		_spec.Modifiers = athq.modifiers
	}
	_spec.Node.Columns = athq.ctx.Fields
	if len(athq.ctx.Fields) > 0 {
		_spec.Unique = athq.ctx.Unique != nil && *athq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, athq.driver, _spec)
}

func (athq *AttributeTypeHistoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(attributetypehistory.Table, attributetypehistory.Columns, sqlgraph.NewFieldSpec(attributetypehistory.FieldID, field.TypeUUID))
	_spec.From = athq.sql
	if unique := athq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if athq.path != nil {
		_spec.Unique = true
	}
	if fields := athq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, attributetypehistory.FieldID)
		for i := range fields {
			if fields[i] != attributetypehistory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := athq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := athq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := athq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := athq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (athq *AttributeTypeHistoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(athq.driver.Dialect())
	t1 := builder.Table(attributetypehistory.Table)
	columns := athq.ctx.Fields
	if len(columns) == 0 {
		columns = attributetypehistory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if athq.sql != nil {
		selector = athq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if athq.ctx.Unique != nil && *athq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range athq.modifiers {
		m(selector)
	}
	for _, p := range athq.predicates {
		p(selector)
	}
	for _, p := range athq.order {
		p(selector)
	}
	if offset := athq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := athq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (athq *AttributeTypeHistoryQuery) ForUpdate(opts ...sql.LockOption) *AttributeTypeHistoryQuery {
	if athq.driver.Dialect() == dialect.Postgres {
		athq.Unique(false)
	}
	athq.modifiers = append(athq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return athq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (athq *AttributeTypeHistoryQuery) ForShare(opts ...sql.LockOption) *AttributeTypeHistoryQuery {
	if athq.driver.Dialect() == dialect.Postgres {
		athq.Unique(false)
	}
	athq.modifiers = append(athq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return athq
}

// AttributeTypeHistoryGroupBy is the group-by builder for AttributeTypeHistory entities.
type AttributeTypeHistoryGroupBy struct {
	selector
	build *AttributeTypeHistoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (athgb *AttributeTypeHistoryGroupBy) Aggregate(fns ...AggregateFunc) *AttributeTypeHistoryGroupBy {
	athgb.fns = append(athgb.fns, fns...)
	return athgb
}

// Scan applies the selector query and scans the result into the given value.
func (athgb *AttributeTypeHistoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, athgb.build.ctx, "GroupBy")
	if err := athgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AttributeTypeHistoryQuery, *AttributeTypeHistoryGroupBy](ctx, athgb.build, athgb, athgb.build.inters, v)
}

func (athgb *AttributeTypeHistoryGroupBy) sqlScan(ctx context.Context, root *AttributeTypeHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(athgb.fns))
	for _, fn := range athgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*athgb.flds)+len(athgb.fns))
		for _, f := range *athgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*athgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := athgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AttributeTypeHistorySelect is the builder for selecting fields of AttributeTypeHistory entities.
type AttributeTypeHistorySelect struct {
	*AttributeTypeHistoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (aths *AttributeTypeHistorySelect) Aggregate(fns ...AggregateFunc) *AttributeTypeHistorySelect {
	aths.fns = append(aths.fns, fns...)
	return aths
}

// Scan applies the selector query and scans the result into the given value.
func (aths *AttributeTypeHistorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aths.ctx, "Select")
	if err := aths.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AttributeTypeHistoryQuery, *AttributeTypeHistorySelect](ctx, aths.AttributeTypeHistoryQuery, aths, aths.inters, v)
}

func (aths *AttributeTypeHistorySelect) sqlScan(ctx context.Context, root *AttributeTypeHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(aths.fns))
	for _, fn := range aths.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*aths.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aths.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
