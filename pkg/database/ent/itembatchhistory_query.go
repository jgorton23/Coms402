// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"

	"git.las.iastate.edu/SeniorDesignComS/2023spr/online-certificate-repo/backend/pkg/database/ent/itembatchhistory"
	"git.las.iastate.edu/SeniorDesignComS/2023spr/online-certificate-repo/backend/pkg/database/ent/predicate"
)

// ItemBatchHistoryQuery is the builder for querying ItemBatchHistory entities.
type ItemBatchHistoryQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.ItemBatchHistory
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ItemBatchHistoryQuery builder.
func (ibhq *ItemBatchHistoryQuery) Where(ps ...predicate.ItemBatchHistory) *ItemBatchHistoryQuery {
	ibhq.predicates = append(ibhq.predicates, ps...)
	return ibhq
}

// Limit the number of records to be returned by this query.
func (ibhq *ItemBatchHistoryQuery) Limit(limit int) *ItemBatchHistoryQuery {
	ibhq.ctx.Limit = &limit
	return ibhq
}

// Offset to start from.
func (ibhq *ItemBatchHistoryQuery) Offset(offset int) *ItemBatchHistoryQuery {
	ibhq.ctx.Offset = &offset
	return ibhq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ibhq *ItemBatchHistoryQuery) Unique(unique bool) *ItemBatchHistoryQuery {
	ibhq.ctx.Unique = &unique
	return ibhq
}

// Order specifies how the records should be ordered.
func (ibhq *ItemBatchHistoryQuery) Order(o ...OrderFunc) *ItemBatchHistoryQuery {
	ibhq.order = append(ibhq.order, o...)
	return ibhq
}

// First returns the first ItemBatchHistory entity from the query.
// Returns a *NotFoundError when no ItemBatchHistory was found.
func (ibhq *ItemBatchHistoryQuery) First(ctx context.Context) (*ItemBatchHistory, error) {
	nodes, err := ibhq.Limit(1).All(setContextOp(ctx, ibhq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{itembatchhistory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ibhq *ItemBatchHistoryQuery) FirstX(ctx context.Context) *ItemBatchHistory {
	node, err := ibhq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ItemBatchHistory ID from the query.
// Returns a *NotFoundError when no ItemBatchHistory ID was found.
func (ibhq *ItemBatchHistoryQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ibhq.Limit(1).IDs(setContextOp(ctx, ibhq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{itembatchhistory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ibhq *ItemBatchHistoryQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := ibhq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ItemBatchHistory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ItemBatchHistory entity is found.
// Returns a *NotFoundError when no ItemBatchHistory entities are found.
func (ibhq *ItemBatchHistoryQuery) Only(ctx context.Context) (*ItemBatchHistory, error) {
	nodes, err := ibhq.Limit(2).All(setContextOp(ctx, ibhq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{itembatchhistory.Label}
	default:
		return nil, &NotSingularError{itembatchhistory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ibhq *ItemBatchHistoryQuery) OnlyX(ctx context.Context) *ItemBatchHistory {
	node, err := ibhq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ItemBatchHistory ID in the query.
// Returns a *NotSingularError when more than one ItemBatchHistory ID is found.
// Returns a *NotFoundError when no entities are found.
func (ibhq *ItemBatchHistoryQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ibhq.Limit(2).IDs(setContextOp(ctx, ibhq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{itembatchhistory.Label}
	default:
		err = &NotSingularError{itembatchhistory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ibhq *ItemBatchHistoryQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := ibhq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ItemBatchHistories.
func (ibhq *ItemBatchHistoryQuery) All(ctx context.Context) ([]*ItemBatchHistory, error) {
	ctx = setContextOp(ctx, ibhq.ctx, "All")
	if err := ibhq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ItemBatchHistory, *ItemBatchHistoryQuery]()
	return withInterceptors[[]*ItemBatchHistory](ctx, ibhq, qr, ibhq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ibhq *ItemBatchHistoryQuery) AllX(ctx context.Context) []*ItemBatchHistory {
	nodes, err := ibhq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ItemBatchHistory IDs.
func (ibhq *ItemBatchHistoryQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if ibhq.ctx.Unique == nil && ibhq.path != nil {
		ibhq.Unique(true)
	}
	ctx = setContextOp(ctx, ibhq.ctx, "IDs")
	if err = ibhq.Select(itembatchhistory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ibhq *ItemBatchHistoryQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := ibhq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ibhq *ItemBatchHistoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ibhq.ctx, "Count")
	if err := ibhq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ibhq, querierCount[*ItemBatchHistoryQuery](), ibhq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ibhq *ItemBatchHistoryQuery) CountX(ctx context.Context) int {
	count, err := ibhq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ibhq *ItemBatchHistoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ibhq.ctx, "Exist")
	switch _, err := ibhq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ibhq *ItemBatchHistoryQuery) ExistX(ctx context.Context) bool {
	exist, err := ibhq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ItemBatchHistoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ibhq *ItemBatchHistoryQuery) Clone() *ItemBatchHistoryQuery {
	if ibhq == nil {
		return nil
	}
	return &ItemBatchHistoryQuery{
		config:     ibhq.config,
		ctx:        ibhq.ctx.Clone(),
		order:      append([]OrderFunc{}, ibhq.order...),
		inters:     append([]Interceptor{}, ibhq.inters...),
		predicates: append([]predicate.ItemBatchHistory{}, ibhq.predicates...),
		// clone intermediate query.
		sql:  ibhq.sql.Clone(),
		path: ibhq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		HistoryTime time.Time `json:"history_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ItemBatchHistory.Query().
//		GroupBy(itembatchhistory.FieldHistoryTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ibhq *ItemBatchHistoryQuery) GroupBy(field string, fields ...string) *ItemBatchHistoryGroupBy {
	ibhq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ItemBatchHistoryGroupBy{build: ibhq}
	grbuild.flds = &ibhq.ctx.Fields
	grbuild.label = itembatchhistory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		HistoryTime time.Time `json:"history_time,omitempty"`
//	}
//
//	client.ItemBatchHistory.Query().
//		Select(itembatchhistory.FieldHistoryTime).
//		Scan(ctx, &v)
func (ibhq *ItemBatchHistoryQuery) Select(fields ...string) *ItemBatchHistorySelect {
	ibhq.ctx.Fields = append(ibhq.ctx.Fields, fields...)
	sbuild := &ItemBatchHistorySelect{ItemBatchHistoryQuery: ibhq}
	sbuild.label = itembatchhistory.Label
	sbuild.flds, sbuild.scan = &ibhq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ItemBatchHistorySelect configured with the given aggregations.
func (ibhq *ItemBatchHistoryQuery) Aggregate(fns ...AggregateFunc) *ItemBatchHistorySelect {
	return ibhq.Select().Aggregate(fns...)
}

func (ibhq *ItemBatchHistoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ibhq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ibhq); err != nil {
				return err
			}
		}
	}
	for _, f := range ibhq.ctx.Fields {
		if !itembatchhistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ibhq.path != nil {
		prev, err := ibhq.path(ctx)
		if err != nil {
			return err
		}
		ibhq.sql = prev
	}
	return nil
}

func (ibhq *ItemBatchHistoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ItemBatchHistory, error) {
	var (
		nodes = []*ItemBatchHistory{}
		_spec = ibhq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ItemBatchHistory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ItemBatchHistory{config: ibhq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(ibhq.modifiers) > 0 {
		_spec.Modifiers = ibhq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ibhq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ibhq *ItemBatchHistoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ibhq.querySpec()
	if len(ibhq.modifiers) > 0 {
		_spec.Modifiers = ibhq.modifiers
	}
	_spec.Node.Columns = ibhq.ctx.Fields
	if len(ibhq.ctx.Fields) > 0 {
		_spec.Unique = ibhq.ctx.Unique != nil && *ibhq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ibhq.driver, _spec)
}

func (ibhq *ItemBatchHistoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(itembatchhistory.Table, itembatchhistory.Columns, sqlgraph.NewFieldSpec(itembatchhistory.FieldID, field.TypeUUID))
	_spec.From = ibhq.sql
	if unique := ibhq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ibhq.path != nil {
		_spec.Unique = true
	}
	if fields := ibhq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, itembatchhistory.FieldID)
		for i := range fields {
			if fields[i] != itembatchhistory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ibhq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ibhq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ibhq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ibhq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ibhq *ItemBatchHistoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ibhq.driver.Dialect())
	t1 := builder.Table(itembatchhistory.Table)
	columns := ibhq.ctx.Fields
	if len(columns) == 0 {
		columns = itembatchhistory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ibhq.sql != nil {
		selector = ibhq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ibhq.ctx.Unique != nil && *ibhq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range ibhq.modifiers {
		m(selector)
	}
	for _, p := range ibhq.predicates {
		p(selector)
	}
	for _, p := range ibhq.order {
		p(selector)
	}
	if offset := ibhq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ibhq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (ibhq *ItemBatchHistoryQuery) ForUpdate(opts ...sql.LockOption) *ItemBatchHistoryQuery {
	if ibhq.driver.Dialect() == dialect.Postgres {
		ibhq.Unique(false)
	}
	ibhq.modifiers = append(ibhq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return ibhq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (ibhq *ItemBatchHistoryQuery) ForShare(opts ...sql.LockOption) *ItemBatchHistoryQuery {
	if ibhq.driver.Dialect() == dialect.Postgres {
		ibhq.Unique(false)
	}
	ibhq.modifiers = append(ibhq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return ibhq
}

// ItemBatchHistoryGroupBy is the group-by builder for ItemBatchHistory entities.
type ItemBatchHistoryGroupBy struct {
	selector
	build *ItemBatchHistoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ibhgb *ItemBatchHistoryGroupBy) Aggregate(fns ...AggregateFunc) *ItemBatchHistoryGroupBy {
	ibhgb.fns = append(ibhgb.fns, fns...)
	return ibhgb
}

// Scan applies the selector query and scans the result into the given value.
func (ibhgb *ItemBatchHistoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ibhgb.build.ctx, "GroupBy")
	if err := ibhgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ItemBatchHistoryQuery, *ItemBatchHistoryGroupBy](ctx, ibhgb.build, ibhgb, ibhgb.build.inters, v)
}

func (ibhgb *ItemBatchHistoryGroupBy) sqlScan(ctx context.Context, root *ItemBatchHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ibhgb.fns))
	for _, fn := range ibhgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ibhgb.flds)+len(ibhgb.fns))
		for _, f := range *ibhgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ibhgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ibhgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ItemBatchHistorySelect is the builder for selecting fields of ItemBatchHistory entities.
type ItemBatchHistorySelect struct {
	*ItemBatchHistoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ibhs *ItemBatchHistorySelect) Aggregate(fns ...AggregateFunc) *ItemBatchHistorySelect {
	ibhs.fns = append(ibhs.fns, fns...)
	return ibhs
}

// Scan applies the selector query and scans the result into the given value.
func (ibhs *ItemBatchHistorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ibhs.ctx, "Select")
	if err := ibhs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ItemBatchHistoryQuery, *ItemBatchHistorySelect](ctx, ibhs.ItemBatchHistoryQuery, ibhs, ibhs.inters, v)
}

func (ibhs *ItemBatchHistorySelect) sqlScan(ctx context.Context, root *ItemBatchHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ibhs.fns))
	for _, fn := range ibhs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ibhs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ibhs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
