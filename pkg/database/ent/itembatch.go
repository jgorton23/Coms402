// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"

	"git.las.iastate.edu/SeniorDesignComS/2023spr/online-certificate-repo/backend/pkg/database/ent/company"
	"git.las.iastate.edu/SeniorDesignComS/2023spr/online-certificate-repo/backend/pkg/database/ent/itembatch"
)

// ItemBatch is the model entity for the ItemBatch schema.
type ItemBatch struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ItemNumber holds the value of the "itemNumber" field.
	ItemNumber string `json:"itemNumber,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// CompanyUUID holds the value of the "companyUUID" field.
	CompanyUUID uuid.UUID `json:"companyUUID,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ItemBatchQuery when eager-loading is set.
	Edges ItemBatchEdges `json:"edges"`
}

// ItemBatchEdges holds the relations/edges for other nodes in the graph.
type ItemBatchEdges struct {
	// Company holds the value of the company edge.
	Company *Company `json:"company,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// CompanyOrErr returns the Company value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ItemBatchEdges) CompanyOrErr() (*Company, error) {
	if e.loadedTypes[0] {
		if e.Company == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: company.Label}
		}
		return e.Company, nil
	}
	return nil, &NotLoadedError{edge: "company"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ItemBatch) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case itembatch.FieldItemNumber, itembatch.FieldDescription:
			values[i] = new(sql.NullString)
		case itembatch.FieldID, itembatch.FieldCompanyUUID:
			values[i] = new(uuid.UUID)
		default:
			return nil, fmt.Errorf("unexpected column %q for type ItemBatch", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ItemBatch fields.
func (ib *ItemBatch) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case itembatch.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				ib.ID = *value
			}
		case itembatch.FieldItemNumber:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field itemNumber", values[i])
			} else if value.Valid {
				ib.ItemNumber = value.String
			}
		case itembatch.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				ib.Description = value.String
			}
		case itembatch.FieldCompanyUUID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field companyUUID", values[i])
			} else if value != nil {
				ib.CompanyUUID = *value
			}
		}
	}
	return nil
}

// QueryCompany queries the "company" edge of the ItemBatch entity.
func (ib *ItemBatch) QueryCompany() *CompanyQuery {
	return NewItemBatchClient(ib.config).QueryCompany(ib)
}

// Update returns a builder for updating this ItemBatch.
// Note that you need to call ItemBatch.Unwrap() before calling this method if this ItemBatch
// was returned from a transaction, and the transaction was committed or rolled back.
func (ib *ItemBatch) Update() *ItemBatchUpdateOne {
	return NewItemBatchClient(ib.config).UpdateOne(ib)
}

// Unwrap unwraps the ItemBatch entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ib *ItemBatch) Unwrap() *ItemBatch {
	_tx, ok := ib.config.driver.(*txDriver)
	if !ok {
		panic("ent: ItemBatch is not a transactional entity")
	}
	ib.config.driver = _tx.drv
	return ib
}

// String implements the fmt.Stringer.
func (ib *ItemBatch) String() string {
	var builder strings.Builder
	builder.WriteString("ItemBatch(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ib.ID))
	builder.WriteString("itemNumber=")
	builder.WriteString(ib.ItemNumber)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(ib.Description)
	builder.WriteString(", ")
	builder.WriteString("companyUUID=")
	builder.WriteString(fmt.Sprintf("%v", ib.CompanyUUID))
	builder.WriteByte(')')
	return builder.String()
}

// ItemBatches is a parsable slice of ItemBatch.
type ItemBatches []*ItemBatch
